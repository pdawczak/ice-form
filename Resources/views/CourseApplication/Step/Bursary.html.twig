{% extends "CourseApplication/Step/default.html.twig" %}

{% block form_content %}
    <p>The Institute offers a number of bursaries, please see the <a href="http://www.ice.cam.ac.uk/studying-with-us/information-for-students/financial-support#loans" target="_blank">Financial Support</a> section for further information.</p>

    <p>The following bursaries are available for this course:</p>
    <p>
        <ul>
            {% if bursaries.irh is defined %}
            <li>
                <strong>{{ bursaries.irh.title }} (&pound;{{ bursaries.irh.value }})</strong> – if you are over 22 and applying to study at university level for the first time.
            </li>
            {% endif %}
            {% if bursaries.js is defined %}
            <li>
                <strong>{{ bursaries.js.title }} (&pound;{{ bursaries.js.value }})</strong> – if you are applying to study at the Institute of Continuing Education for the first time.
            </li>
            {% endif %}
            {% if bursaries.cup is defined %}
            <li>
                <strong>{{ bursaries.cup.title }} (&pound;{{ bursaries.cup.value }})</strong> – if you teach at a UK state school or state-funded further education institution.
            </li>
            {% endif %}
        </ul>
    </p>


    {{ form_row(form.bursary.bursaryWishToApply, {'form_type':'horizontal'}) }}

    <div id="whenBursaryWishToApply">
        <p><em>Please note that bursaries are awarded at the Institute’s discretion on a first-come, first-served basis. Answering the questions below does not automatically guarantee a bursary place.</em></p>

        {%  if form.bursary.previousICEStudy is defined  %}
        {{ form_row(form.bursary.previousICEStudy, {'form_type':'horizontal'}) }}
        {% endif %}

        {%  if form.bursary.ukSchoolTeaching is defined  %}
        {{ form_row(form.bursary.ukSchoolTeaching, {'form_type':'horizontal'}) }}
        <div id="whenUkSchool">
            {{ form_row(form.bursary.schoolContact) }}
        </div>
        {% endif %}

        {%  if form.bursary.previousUniStudy is defined  %}
        {{ form_row(form.bursary.previousUniStudy, {'form_type':'horizontal'}) }}
        <div id="whenIRH">
            {{ form_row(form.bursary.irhStatement) }}
        </div>
        {% endif %}
    </div>

    {{ form_widget(form) }}
    {{ form_rest(form) }}
{% endblock %}
